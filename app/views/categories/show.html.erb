<div class="container category-show-content">
  <h1><%= @category.title %></h1>
  <div class="row my-4">
    <div class="subsidies-list col-md-7 px-3">
      <%= render 'list_of_categories' %>
      <div class="back-button mt-4">
        <%= link_to 'カテゴリ一覧へ戻る', categories_path, class: "btn btn-primary" %>
      </div>
    </div>
    <div class="side_bar col-md-5 px-3">
      <%= render 'shared/side_bar' %>
    </div>
  </div>
</div>